name: Fallback FTS residue regressions
on:
  push:
    branches: [main]
    paths: ['.github/workflows/fts-residue-regression.yml']
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: fts-residue-regression-${{ github.ref }}
  cancel-in-progress: true
jobs:
  regression:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      CARGO_BUILD_JOBS: '2'
      CARGO_PROFILE_DEV_DEBUG: '0'
      CARGO_PROFILE_TEST_DEBUG: '0'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Install pinned toolchain
        run: rustup show active-toolchain
      - name: Apply checksum-verified reviewed patch
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import base64, gzip, hashlib, pathlib, re, subprocess
          transport = subprocess.check_output(['git', 'show', 'e5a882e8eefb22ded2ea5bb557af0e7c007f8122:.github/workflows/fts-residue-regression.yml'], text=True)
          payload = re.search(r"printf '%s' '([^']+)'", transport).group(1)
          # Repair four transcription errors in the immutable transport;
          # authenticate the resulting exact Git patch before applying it.
          for start, end, old, new in reversed([(510,511,'e',''), (1801,1803,'FS',''), (12044,12044,'','V'), (16657,16657,'','/')]):
              assert payload[start:end] == old
              payload = payload[:start] + new + payload[end:]
          patch = gzip.decompress(base64.b64decode(payload, validate=True))
          assert hashlib.sha256(patch).hexdigest() == '5f059132ffdb8d2d3f638386b2e88ff8199ee70e50308adb82c28ae12d9c0ce0'
          subprocess.run(['git', 'apply', '--check', '-'], input=patch, check=True)
          subprocess.run(['git', 'apply', '-'], input=patch, check=True)
          PY
          rustfmt --edition 2024 --config skip_children=true src/storage/sqlite.rs src/doctor_recover.rs src/indexer/mod.rs src/lib.rs
          git diff --check
          git diff --stat
      - uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      - name: Run residue and convergence regressions
        run: |
          set -o pipefail
          cargo test --locked --lib fts_residue -- --test-threads=1 2>&1 | tee fts-residue-test.log
          cargo test --locked --lib fts_shadow_ -- --test-threads=1 2>&1 | tee fts-shadow-test.log
          cargo test --locked --lib fts_contentless_docsize -- --test-threads=1 2>&1 | tee fts-contentless-test.log
          cargo test --locked --lib fts_repair_refuses_an_active_index_lease -- --test-threads=1 2>&1 | tee fts-lock-test.log
          cargo test --locked --lib fallback_fts -- --test-threads=1 2>&1 | tee fallback-fts-test.log
      - name: Publish repair directly to main
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/storage/sqlite.rs src/doctor_recover.rs src/indexer/mod.rs src/lib.rs
          git diff --cached --check
          git commit -m 'fix(fts): repair residue and converge retired fallback shadows (#495)'
          git push origin HEAD:main
      - name: Preserve exact patched sources and test output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fts-repair-sources
          retention-days: 3
          include-hidden-files: true
          path: |
            src/storage/
            src/doctor_recover.rs
            src/indexer/mod.rs
            src/lib.rs
            src/franken_sync.rs
            src/model/types.rs
            Cargo.toml
            Cargo.lock
            rust-toolchain.toml
            .cargo/config.toml
            AGENTS.md
            *fts*test.log
